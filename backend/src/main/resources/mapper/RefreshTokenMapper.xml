<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.authboard.mapper.RefreshTokenMapper">

    <select id="findByToken" resultType="RefreshToken">
        SELECT id, member_id, token, expires_at, created_at
        FROM refresh_token
        WHERE token = #{token}
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO refresh_token (member_id, token, expires_at)
        VALUES (#{memberId}, #{token}, #{expiresAt})
    </insert>

    <delete id="deleteByMemberId">
        DELETE FROM refresh_token
        WHERE member_id = #{memberId}
    </delete>

</mapper>
